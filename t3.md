You are creating a ultra-compact interview cheat sheet in Markdown for printing.

INPUT: A list of ML coding questions and LeetCode problems organized by pattern/category.

For EACH question without skipping any, output exactly this format:

---
**[Problem Name]** — [one-line core idea, max 12 words]
`key insight` → dry run: [1-2 line concrete tiny example, e.g. arr=[1,2,3] → output]
```code``` ← minimal working solution, no comments, no blank lines, renamed vars to shortest possible
---

RULES:
- Zero skips. Every single problem must appear.
- No intros, no conclusions, no section headers beyond the category name.
- Code blocks: shortest correct solution only. No docstrings, no type hints, no blank lines inside.
- Dry run: use the smallest possible input that shows the logic (3-5 elements max).
- Core idea: must fit in 12 words. Cut adjectives.
- If a problem has a classic trick (two pointers, monotonic stack, etc.), put it in `backticks` before the dry run.
- For ML implementations: show the math formula inline as a comment on the first line, then the code.
- Target: each problem fits in 6-10 printed lines. No exceptions.
- Font: assume monospace 9pt, A4 paper, 2-column layout when printed.
- Output pure Markdown only. No HTML. No extra blank lines between problems.
```

---

**Tips for best output:**
- Feed it one category at a time (e.g. paste just the Sliding Window section) — Copilot context is limited and quality drops on large inputs
- After each category, say `"continue, same format, no skips"` to keep it on track
- If it starts padding or skipping, say `"too verbose, max 8 lines per problem, no skips"`
- At the end, ask it to `"add a 1-page summary: all patterns + their trigger conditions, 1 line each"`
